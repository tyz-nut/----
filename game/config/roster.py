"""所有角色的数值。想调平衡只改这个文件。

这里只有数字和文案，一行行为逻辑都没有——角色"撞上对手发生什么、技能放出来是
什么效果"写在 game/characters/ 里。加角色的顺序是：先在那个目录里写好类（碰撞
效果进 base/normal/vampire，技能进 skills.py），再回到这里补一段数值。

几个会互相牵连的数，改一个记得看另一个：

- **撞击伤害只有普通小球有**。它不算通用属性，而是普通小球自己写的一条碰撞
  效果（game/characters/normal.py），基类的默认碰撞效果只是"弹开"——另外三个
  角色撞人都只弹开、不掉血，它们各自的输出在别处（吸住吸取 / 钩锁 / 墙上的线）。

  普通小球那条伤害 = 撞上瞬间的自身移速的**平方** × impact_damage_per_speed_sq。
  平方意味着"快"是双重收益：移速是攻击力，也是攻击力的平方。移速范围由
  config.BALL_SPEED_MIN / MAX 决定，所以改那两个数等于改普通小球的输出。

  下表按**当前系数 0.0005** 算的，改系数就按比例缩放（系数翻倍，下表全翻倍）：

      速度 220 → 24 点      速度 300 → 45 点
      速度 380 → 72 点      速度 600 → 180 点
      速度 760 → 289 点     速度 950 → 451 点

  按这条曲线，速度攒到 950 左右就是一击一条命（满血 500）。

- 加速是**永久叠加**的，而且每档加的是**绝对速度**，不是倍率：加
  出生速度 × spawn_speed_gain（默认 0.5，也就是半条出生速度）。
  出生速度是每个球出生时随机抽的，范围在 config.BALL_SPEED_MIN / MAX
  （220~380），所以每档大约加 110~190 px/s——攒到第 3 档就已经超过出生速度本身了。
  它和上面那条平方伤害是乘在一起的，所以攒几档之后伤害涨得远比速度看着快。

- 吸血鬼不吃撞击这一套：它的输出来自吸住（latch_seconds / drain_per_second）
  和蝙蝠圈（BatSwarmSkill 那几项），撞上对手的那一瞬间不掉血。想让它撞人也
  掉血，得去 game/characters/vampire.py 的 on_collision 里填 damage_to_other
  （抓取只顶替弹开，不影响撞击伤害结算，两者可以并存）。

- max_hp 和 drain_per_second 要一起看。真正决定"吸满一轮吸掉对方多少"的是
  latch_seconds × drain_per_second 相对 max_hp 的比例，不是单独的某一个。
  踩过的坑：max_hp 从 100 提到 500 时忘了同步放大 drain，一轮只吸掉 4%，
  对局永远打不完；反过来两者凑巧相等（50 秒 × 10/秒 = 满血 500），就变成
  一次抓取直接定生死。

- 蝙蝠圈的吸血和吸住的吸血是两路，会同时生效、各自结算，可以简单相加。
  所以两道输出都要单独看，别只按一路估。

- **冷却只在技能生效结束之后才开始走**。一次性技能（加速，duration = 0）放出来
  就立刻进冷却；持续型技能（蝙蝠圈，duration = 5）先走完那 5 秒，这 5 秒不算冷却。
  所以吸血鬼"冷却 15 + 持续 5"实际是每 20 秒一轮，而不是每 15 秒一轮——想把
  节奏调成 15 秒一轮，就要把 cooldown 填成 10。条上是先黄（生效）后蓝（冷却）。

- 连体位移（吸住）和沉默是两个**独立**的通用机制，吸血鬼只是碰巧两个都用了：
  抓取在 CollisionOutcome.grab_seconds，沉默在 CollisionOutcome.silences，
  想只抓不封就把 silences 去掉。沉默没有时长，绑多久封多久——它跟着抓取一起
  开始、一起结束。想只封不绑得由技能来给（技能有生命周期，能自己解封）。

  封技能封的是"开"这个动作，**不会打断已经放出来的**：生效中的技能照常走完，
  冷却照常在后台走。所以 latch_seconds 越长，压制力越强——它不只是吸一段血，
  还是替你把对方的技能窗口往后推。

- 霸体（渔夫甩着钩锁时）只顶替**被推动**，伤害照常两边各算各的。所以高速小球
  撞上定住的渔夫：小球按自己的速度算出伤害打给渔夫，渔夫自己那份是 0，
  于是"渔夫掉血、小球不掉"。和"抓取只顶替弹开、不免除伤害"是同一条原则。

  霸体挡的是"被推动"，不是"被碰上"，所以**抓取照样抓得上**——只是抓到手之后
  两边都动不了：合体速度按无穷大质量算（见 Match.combined_velocity），渔夫
  静止，于是吸血鬼也停在原地。蝙蝠圈一样照常减速和吸血（光环在钩锁之前结算）。

  定在原地的渔夫**此刻的动量是零**（见 Ball.effective_velocity）：velocity 里
  存的那份只是"松手之后接着飞的那份"。所以锤子砸上来时锤头那一下没有他的速度
  垫底，刺客闪到他身后接过的也是 0。

- 钩锁飞出去期间渔夫一直定在原地，所以**飞多久**是这条技能唯一要小心的数。
  鱼线是有限长的（line_length），走到头这一发就结束——不再是"一直弹墙直到
  钩中人为止"，也就没有罚站这回事了。于是 hook_speed 和 line_length 要一起看：
  线乘出来的是**射程**（能管到多远），速度决定的是**飞完这一程要几秒**
  （也就是渔夫要定住多久）。线拉长而速度不变，罚站时间跟着线性变长。

  线走到头没钩到人就转成放鱼（见下），所以"钩空"不等于白甩；连鱼都没召唤
  出来的那一档（fish.fail_chance）才是真的白甩一次。

  钩中之后**对方被沉默**，一直到松口为止（Match.hook_hit / release_hook）。
  和吸住那条沉默是同一个机制，封的只是"开"这个动作：已经在生效的技能照常走完，
  冷却照常在后台走。所以钩住越久，越是在替你把对方的技能窗口往后推。

- 激光是**被动**技能，没有冷却也没有生效时长，skill 那一项里只有
  damage_per_second 一个数有用（cooldown 填 0 只是为了让基类构造得出来）。
  它的规则是撞墙的**开合**：普通模式撞墙 → 进入连线模式并攒下这个墙面上的点；
  连线模式撞**另一面**墙 → 两点连成一条激光、退回普通模式；撞**同一面**墙 →
  只是把攒着的点挪到新位置（贴着墙掠过去会在同一面留下很多下，都另起一个的话
  线不知道从哪出发）。

  所以输出节奏完全由"它多久能撞到两面不同的墙"决定，没有可以调的冷却。线是
  **永久**的、**不封顶**的，攒到后面战场上会同时挂着好几根——这是这个角色唯一的
  成长曲线：活得越久、线越多、对手能站的地方越少。

  代价是它**前期几乎没输出**：开局一根线都没有，全靠自己撞出第一根。又因为
  每根线都要"两次撞墙"才成型，中间那一下（连线模式）是没有任何收益的——所以
  damage_per_second 这个数要按"通常只会压到一两根"来估，不用往低了填。

  线的判定是"球心到线段的距离 ≤ 对方半径"，也就是画的线真挨上球面才作数
  （线的两头都在**墙面**上，不是球心，所以贴墙走的球也能吃到——球心最多离墙
  一个半径，正好卡在判定边上）。

  自己的线不伤自己。线挂在 owner 身上（ball.lasers），重开、换角色都会清掉。

- 武士是**唯一有两个技能的角色**：一个常驻被动（绕身刀，填在 passive 那一栏）、
  一个主动（穿刺，填在 skill 那一栏）。被动不占技能条——角色卡和技能条都只认
  skill，passive 是"生来就有"的配件，由 Character.attach_passives 在造球时装上，
  之后它自己一直在，没有冷却也不会被"放"。

  绕身刀的每圈伤害：刀是一个从球心往外伸的线段（inner_radius → outer_radius），
  以 angular_speed 转。判定是"球心到这条线段的最短距离 ≤ 对方半径"，所以刀的
  **长度**也是攻击范围的一部分。每转满一圈才清一次"这轮打过谁"的名单，也就是
  一个敌人**每圈最多挨一下**——转多快都不会变成连续掉血，圈的时长（2π/转速）
  才是一次伤害的间隔。

  穿刺这条线要连着看三个数：thrust_speed 决定对方有多少时间让开（越慢越好躲），
  thrust_distance 决定够不够得着（方向出手时就锁死了，敌人跑出这条线就落空），
  damage 是这一下的全部收益。它**不是必中的锁定技**，一次冲过头就得等冷却。

  够着了就停在对方身前收招，所以 thrust_distance 是**射程**，不是"一定会走完
  的路程"：打中的那一次往往冲不到头。撞墙同理，贴着墙出手等于白放。

- 蜘蛛也是**被动**，和激光同类（撞墙触发，走 on_wall_hit）。区别在线的两头：
  激光那条线两端都钉在墙上，跟人无关；蛛丝**一头钉墙、一头永远是蜘蛛本人**，
  所以蜘蛛一跑，整把丝就跟着扫过去。

  这决定了蛛丝的威胁是"以自己为顶点的一把扇子"而不是场地上几条固定的线，
  也决定了它**刚钉下时几乎没有用**（锚点就在脚下那面墙上，丝只有一个半径长），
  要跑开才拉得开。所以 damage_per_second 要按"跑起来之后同时压到几根"来估。

  减速**不逐根相乘**：几根丝各乘一遍就是指数级地慢，两三下就贴死在原地了。
  取最狠的那一根，伤害才叠加（见 Match.apply_webs）。

- 死灵法师是两个**配套**的机制，单看哪一个都不成立：

  碰撞伤害算的是**对方已损**的血量（missing_hp_damage_ratio 乘的是 other，不是
  自己），所以对方满血时这一下是 0——它不是拿来削血的，是拿来收残血的。这也
  意味着它自己几乎不靠撞人建立优势，得等对方被别的东西磨下去。

  黑夜降临（NightfallSkill）正好补上这一环：双方**互换位置和血量**。血是按时
  间轴的"在哪一瞬"结算的——两球都活着、黑屏黑到底的那一瞬间结算一次，之后
  各走各的（见 Match.nightfall_swap）。所以它是"先被打残、再换过来"，一个
  稳定的翻盘手段，而不是一个输出技能。

  而且它**只在放的人血量百分比低于对手时才真的换**：不落后就什么都不换，
  黑屏照播（纯演出）。所以它是一张专门留给残血的牌，健康的时候放等于白费一次
  冷却；平手也不换。这是"落后"两个字唯一落地的地方，改 nightfall_swap 时
  别只盯着位置那一坨。

  换的是**血量百分比**，不是血量数字：按比例换（各换成对方的 hp_ratio × 自己的
  max_hp）。两个角色 max_hp 不一样时，直接换数字会让一方凭空多出或少掉一截血。

  **位置换不换要看当时的状态**，不是无条件的：两球里只要有任何一个的位置不由
  自己说了算（霸体的渔夫、被吸住的那一对、正在被钩锁拖的人），位置就不换，
  只换血。理由很直接——硬把一颗被钩住的球挪走，钩索会当场断在半空。穿刺
  **不算**这一条：它只是自己跑一段，随时都能停，不欠着谁的。

  cooldown **从黑屏结束那一刻**才开始走（它有 duration），所以两场黑夜之间
  实际隔的是 cooldown + duration。整段黑屏多长是 duration，三段（渐暗 / 全黑 /
  渐亮）各占多少是 config/settings.py 里的三个 ratio，加起来必须是 1——换位卡
  在渐暗走完的那一瞬，rise 太短会看到球当场挪窝，太长又显得拖。

- 大锤的锤子和武士那把刀是**同一套几何**（绕着自己转、每圈最多命中一次），
  差别全在命中之后：刀是蹭一次血，锤子是**把对方整个速度重写**——概念上是
  对方撞上了一面**正在移动的无限质量的墙**，完全弹性。推到公式是
  v' = 2u − v（u 是锤头速度），推导和"为什么它不守恒动量"写在
  game/states/hammer.py 的开头。

  这是全场唯一一个**直接改对方速度**的东西，所以它和别的输出不是一回事：
  别的角色是"打得疼"，大锤是"打得远"。挨一锤的人飞出去撞墙那一下还会按
  撞墙的规矩再吃一次（速度越快越疼），这才是这个角色真正的输出来源。

  u 是**锤头**的速度，不是大锤本人：锤头一边跟着球平移，一边绕着球转，所以

      u = 球的实际速度 + 角速度 × outer_radius（切向）

  于是 angular_speed、outer_radius、以及大锤自己飞多快，三样都进 u，而伤害
  又是相对速度的**平方**——这三样全是双重收益。特别是 outer_radius：它同时
  决定够不够得着、锤头有多快、把人打飞多远，是这一套里最敏感的一个数。

  打击判定只认**锤头**不认柄（锤头圆心距对方球心 ≤ head_radius + 对方半径），
  所以贴着球边擦过去的敌人不算挨锤。柄是握的地方，砸人的是头。

  挨锤的人里**霸体那几种打不飞，但照常掉血**（霸体顶替的是"被推动"，不是
  "被碰上"，和渔夫那条同源）。它们会一直待在锤子的道上，全靠 hammer_ 那个
  "每圈一次"的名单兜着——不封顶的话一秒 60 锤。

  速度没有上限，所以这一锤**有可能把球甩得比 MAX_FRAME_TIME 那条安全线还快**
  （config/settings.py 里那段 556px 的推导）。真甩出去了就是穿透墙面飞出场外，
  那一条只影响"某局莫名其妙打了很久"时的观感，先记在这儿。

- 毒刺和武士一样是**两个技能**：被动的刺填在 passive，主动的毒发填在 skill。
  两个技能咬合得很紧，单看哪一个都不成立：

  刺是撞墙触发的（和激光、蛛丝同类），但它的触发方式是三者里独一份——
  激光要撞两面墙才连出一条线，蛛丝一头连着自己会跟着扫，而刺是**钉死在墙上
  且会反复触发**的：扎完一次过 spike_cooldown 秒重新长好，同一根刺能扎同一个
  人很多次。所以刺不是地雷，是一台一直在出毒的机器。

  于是叠毒速度 = **对手在刺旁边待多久 ÷ spike_cooldown**，不是刺的数量。
  被逼到墙上、被钩锁拖回来贴着墙走、被刀逼着退到墙边，都会在几秒之内叠起
  好几层。6 秒 / 1.5 秒 = 一根刺最多同时挂着 4 层，这是这个数对的账。

  中毒是**每层各走各的倒计时**（用户定的）：连挨三下就是三个错开的沙漏，
  最先扎的先漏完。所以层数会自己褪下去，停下来就没了——它不是"挂着一层毒
  然后一直变长"那种。层数**没有上限**：真叠到十几层，按 poison_per_second
  算自然就疼得离谱了，不需要再设一个盖子。

  毒发是主动技能（有冷却、一次性结算），**按对手此刻的毒层数**造成伤害并回
  自己的血，两样各带一个基础值——所以 0 层时也放得出，只是那一下最轻。

  它是**只读不消耗**的（用户定的）：层数照常走自己的倒计时，毒发只是"看一眼
  现在有几层"。所以这是纯输出技——毒叠上去就一直在高点，不存在"放完要重新叠"
  的节奏。它真正的意义是把刺攒出来的优势一次性兑现，而不是一个消耗品。

  毒刺**永久不封顶**（用户定的，同激光和蛛丝）。所以后期墙上会积很多根，
  对手能贴墙走的地方越来越少——这是它唯一的成长曲线，活得越久越强。

- 望是**唯一一个按时间自己动的角色**：激光、蛛丝、毒刺的被动都等"撞墙"这件事
  发生，望的棋不需要任何触发，钟一到就落一手。所以它不是"越攒越多"（那三个是），
  而是**一直在流**——子踩过就消失，棋盘始终是半空的（见下面那条）。

  它把战场划成 GO_BOARD_SIZE × GO_BOARD_SIZE 的方格（config/settings.py 里
  那个数，用户要求可以自由调）。一手棋是一串而不是一下：黑子随机落在任意一个
  空格上，随后四颗白子**以短得多的间隔一颗接一颗**跟在它的上下左右四格上，
  四颗铺完才进冷却：

      黑 ─0.2s─ 白 ─0.2s─ 白 ─0.2s─ 白 ─0.2s─ 白 ──[interval]── 黑 ...

  所以有两个数：white_interval 是白子之间的那四下有多快，interval 是一手落完
  之后等多久起下一手。一轮的长度是 **4 × white_interval + interval**，不是
  5 × interval。

  interval 决定的是棋盘上**同时**有几颗子，而不是"总共能攒多少"——因为子**踩过
  就消失**（用户定的），棋盘一直是半空的。所以望给对手的压力全在落子的节奏上：
  间隔越短，对手每一步越容易踩上。这是它和激光 / 蛛丝 / 毒刺那三个"永久不封顶"
  的被动最大的不同，改这个数之前先想清楚要的是哪种压力。

  白子的落点规则里藏着一条**自己长出来的节奏**：四邻里已经有子的那几格不进队列，
  黑子落在边角上也没那么多邻格。所以棋盘越满，白子越少——而白子是"配菜"，它的
  用处是把黑子周围那一圈都变成雷区，自己并不疼。换句话说棋盘越挤，剩下的越是
  黑子，越狠。

  黑白都炸：踩上去掉血 **+ 减速**，减速不叠加（取更狠的那一份，同蛛丝那几根）。
  **黑子掉血多**，白子只是陪衬。

  它也是唯一一个"出生只挂节奏、效果每帧由 Match 推"的被动（Match.apply_go）：
  想调强度动的是 interval 和那三个数值，没有可以改的冷却。

  自己的子不炸自己，踩上去也不消耗（同激光、蛛丝、毒刺）：子只对对手有效。

- 国王是**唯一一个"撞人是亏本买卖"的角色**：它撞上敌人伤对方一点、自己掉更多
  （collision_damage / collision_self_damage）。所以它和普通小球是反着来的——
  普通小球是"撞得越狠越赚"，国王是"撞上去就是亏"。它的输出全在**召出来的骑士**
  身上，本体应该躲着走。

  骑士是**一颗完整的球**，不是"技能的效果"：有血量、有自己的动量、撞墙会弹、
  会被别人的技能打到（渔夫的钩锁也钩得到它）。和主球不一样的地方（见
  characters/knight.py）：和自己一边的撞上只弹开不掉血；不参与抓取；不参与
  胜负、黑夜换位、重开重建。

  它和国王那一条正好互补：骑士**伤敌多、自损少**（把骑士拿去换是划算的），
  国王**伤敌少、自损多**（本体去换是亏的）。两个数要一起看——真正决定这个
  角色强不强的是"骑士换掉对方多少血，而重新造一批要等多久"。

  **骑士只有被打死才消失**（用户定的），没有存活时长，所以它们会一直累加。
  这意味着 cooldown 是这个角色唯一的**数量**旋钮：调短它，场上骑士会越堆越多，
  没有别的机制能把它们收回去。knight.max_hp（30）是唯一的闸门——对方打得动
  骑士，这一套就还成立；打不动，国王就是无限滚雪球。

  knight_hp 和 count 也要一起看：三个骑士一起撞上去的伤害是相加的，所以
  count 翻倍等于每一批的输出翻倍，而不是"更耐打一点"。

  launch_speed 只决定骑士**出场那一下**有多冲。骑士之后一直按着那个速度飞
  （撞墙只改方向），所以它同时也是骑士满场跑的节奏：调快，骑士铺得到处都是、
  但撞上谁全看运气；调慢，它们会在国王身边转悠，输出反而更集中。
"""

from ..characters import (
    BatSwarmSkill,
    BladeSkill,
    BlinkStrikeSkill,
    BoostSkill,
    GoSkill,
    HammerSkill,
    HookSkill,
    IdleSkill,
    KingSkill,
    LaserSkill,
    NightfallSkill,
    ThrustSkill,
    VenomSpikeSkill,
    VirulenceSkill,
    WebSkill,
)
from ..characters.assassin import PhantomAssassin
from ..characters.fisher import Fisher
from ..characters.hammer import Hammer
from ..characters.king import King
from ..characters.knight import Knight
from ..characters.laser import Laser
from ..characters.lookout import Lookout
from ..characters.necromancer import Necromancer
from ..characters.normal import NormalBall
from ..characters.samurai import Samurai
from ..characters.spider import Spider
from ..characters.vampire import Vampire
from ..characters.venom import VenomSting
from ..states.hook import FishSpec

# ============================================================
# 普通小球 —— 碰撞走基类的默认行为，技能走通用的加速
# ============================================================
NORMAL = NormalBall(
    id="normal",
    name="普通小球",
    radius=22,                     # 半径（像素）。同时决定碰撞质量（正比于半径的平方）
    max_hp=500,
    description="速度撞击 · 越撞越快",   # 角色卡上的第二行小字
    skill=BoostSkill(
        name="加速",
        cooldown=10.0,             # 冷却秒数，从释放那一刻开始算
        spawn_speed_gain=0.5,      # 每释放一次永久加 出生速度×0.5（绝对值，可叠加）
    ),
    # 撞一次掉的血 = 撞上瞬间的自身移速² × 这个系数。
    # 注意那个速度是**撞上那一刻**的，平飞的时候并不掉血
    impact_damage_per_speed_sq=0.0003,
)

# ============================================================
# 吸血鬼 —— 碰撞改成吸住，技能是范围蝙蝠圈
# ============================================================
VAMPIRE = Vampire(
    id="vampire",
    name="吸血鬼",
    radius=22,
    max_hp=500,
    description="吸住吸取 · 化作蝙蝠",
    skill=BatSwarmSkill(
        name="化作蝙蝠",
        cooldown=15.0,             # 冷却秒数
        duration=5.0,              # 圈持续秒数
        radius=300.0,              # 圈的半径（像素）。战场宽 600，能盖住相当一片
        slow_ratio=0.3,            # 圈内敌人减速比例，0.5 就是速度砍半
        drain_per_second=20.0,     # 圈内每秒从敌人身上吸走多少血（5 秒共 125）
    ),
    # 没有撞击伤害这一项：吸血鬼的碰撞效果整条写在 game/characters/vampire.py
    # 里，撞上对手只抓不撞，输出全在"吸"上。
    latch_seconds=3.0,             # 吸住持续秒数
    drain_per_second=20.0,         # 吸住期间每秒从对方身上吸走多少血
)

# ============================================================
# 渔夫 —— 碰撞效果无（正常弹开、不造成伤害），全靠钩锁输出。
# 钩锁飞出去的路是**有限长**的，走完还没钩到人就转成"从钩子那儿放一条鱼"，
# 所以甩空不是纯亏，只是换来一个体型随机的次级收益（有时连鱼都放不出来）
# ============================================================
FISHER = Fisher(
    id="fisher",
    name="渔夫",
    radius=22,
    max_hp=500,
    description="抛钩拉人 · 定身霸体",
    skill=HookSkill(
        name="抛钩",
        # 冷却**从收招那一刻**才开始算，钩锁飞了多久不计入。钩锁可能飞很久，
        # 所以这里填的是"收招之后再等多久"，不是"两钩之间隔多久"。
        # "收招"包含放鱼那一下——鱼一放出来技能就结束了（用户定的"召出鱼技能
        # 就可以进入冷却了"），所以鱼游不游得到人都不影响这个数。这也意味着
        # 冷却走完就能再甩一钩，而水里那条鱼可能还没咬完
        cooldown=8.0,
        hook_speed=400.0,          # 钩锁飞出去的速度（像素/秒）。和 line_length
                                   # 一起决定罚站多久，见下面那条
        pull_speed=800.0,          # 收线速度（像素/秒）
        drain_per_second=20.0,     # 收线期间每秒从敌人身上吸走多少血
        reel_gap=8.0,              # 拉到身前时两球留的空隙（像素）
        # 鱼线总长（像素，**折线**总长不是直线距离，弹墙也算进去）。走到头
        # 还没钩到人就钩空了，转成放鱼。这个数直接决定"渔夫能管到多大的范围"：
        # 小了够不着人、大了就等于没有限制。
        #
        # 900 比战场的对角线还长（600×600 的场，对角线约 849 像素），所以现在
        # 没有"钩够不着"这一说了——想去哪就去哪，弹几次墙总能到。代价看上面那
        # 一条：飞完这一程要 900÷400 = 2.25 秒，也就是渔夫要**定住两秒多**，
        # 这就是加长射程真正要付的钱。嫌罚站久就把 hook_speed 一起提上去
        line_length=900.0,
        # ---- 钩空之后那条鱼（见 states/fish.py）----
        # 体型在 [min, max] 里均匀随机，伤害和持续时间**都正比于体型**，
        # 所以这两个比例就是这一发收益的全部浮动来源：小鱼扑一下就没，
        # 大鱼咬得重、还能多追一会儿
        fish=FishSpec(
            min_size=9.0,              # 体型下限（像素半径）
            max_size=24.0,             # 体型上限
            damage_per_size=3.0,       # 每 1 像素体型咬多少血
            duration_per_size=0.12,    # 每 1 像素体型活多少秒
            speed=480.0,               # 游速（像素/秒）。比大多数球快，是"冲上去
                                       # 咬一口"的快弹
            turn_rate=4.2,             # 每秒最多转多少弧度
            # 满舵转弯只剩一成四速度。**它和 speed 是一对，别单独调**：转弯半径
            # = speed×(1-turn_slow)÷turn_rate = 480×0.14÷4.2 ≈ 16 像素，得小于
            # 咬合距离（最小的一档是小鱼 r9 + 骑士 r12 = 21）。半径一旦比咬合距离
            # 大，鱼就只会绕着敌人画圈，永远咬不着——它转得再准也没用。speed 往上
            # 提多少，这个数就得跟着往上提多少，半径才不会跟着涨出去
            turn_slow=0.86,
            fail_chance=0.3,           # 钩空之后连鱼都召唤不出来的概率
        ),
    ),
    # 没有撞击伤害这一项：渔夫不覆盖碰撞效果，走基类的默认行为，
    # 也就是正常弹开、不掉血。注意这跟"撞上霸体渔夫时渔夫掉血"是两回事（见下）：
    # 那一下的伤害来自**对方**的碰撞效果，跟渔夫自己有没有输出无关。
)

# ============================================================
# 激光 —— 碰撞效果无，靠撞墙画出来的线割人
# ============================================================
LASER = Laser(
    id="laser",
    name="激光",
    radius=22,
    max_hp=500,
    description="撞墙布线 · 被动成阵",
    skill=LaserSkill(
        name="激光",
        # 被动技能用不到这个数（LaserSkill.ready 恒为 False，永远不会被"放"），
        # 但 Skill 基类要求有；填 0 是最不容易误读的
        cooldown=0.0,
        damage_per_second=25.0,    # 压在一根线上每秒掉多少血。**多根叠加**，
                                   # 所以这个数要按"通常只会压到一两根"来估
    ),
    # 没有撞击伤害这一项：和渔夫一样，走基类默认的碰撞效果——正常弹开、不掉血。
    # 输出全在撞墙画出来的那些线上，跟撞人没关系
)

# ============================================================
# 武士 —— 碰撞效果无，输出在两个技能上：常驻的绕身刀 + 主动的穿刺
# ============================================================
SAMURAI = Samurai(
    id="samurai",
    name="武士",
    radius=22,
    max_hp=500,
    description="绕身刀 · 直线穿刺",
    skill=ThrustSkill(
        name="穿刺",
        cooldown=6.0,              # 冷却秒数。冲刺本身是瞬时结算的（duration = 0），
                                   # 所以冲完立刻开始走冷却
        thrust_speed=1600.0,       # 冲刺速度（像素/秒）。正常移速的三到八倍，
                                   # 对方基本来不及让开——这是这个技能的主要命中来源
        thrust_distance=320.0,     # 一次冲多远（像素）。撞墙就在墙撞人前停下
        damage=60.0,               # 冲到了扣这么多，一次穿刺只扣一下
    ),
    # 常驻被动：不占技能条，也不吃冷却。转速和伤害填在这里
    passive=BladeSkill(
        name="绕身刀",
        cooldown=0.0,              # 被动用不到（ready 恒为 False），填 0 最不容易误读
        angular_speed=4.0,         # 角速度（弧度/秒）。2π/4 ≈ 1.6 秒转一圈，
                                   # 也就是每个敌人每 1.6 秒最多挨一下
        inner_radius=18.0,         # 刀刃内端离球心多远（贴着球面，略小于半径 22）
        outer_radius=160.0,        # 刀刃外端
        damage=25.0,               # 蹭一下扣多少血
    ),
    # 没有撞击伤害这一项：和渔夫、激光一样走基类默认的碰撞效果——正常弹开、
    # 不掉血。撞人本身不输出，输出全在刀和穿刺上
)

# ============================================================
# 蜘蛛 —— 碰撞效果无，靠撞墙拉出来的蛛丝输出
# ============================================================
SPIDER = Spider(
    id="spider",
    name="蜘蛛",
    radius=22,
    max_hp=500,
    description="撞墙拉丝 · 缠身减速",
    skill=WebSkill(
        name="结网",
        # 被动技能用不到这个数（WebSkill.ready 恒为 False，永远不会被"放"），
        # 但 Skill 基类要求有；填 0 是最不容易误读的
        cooldown=0.0,
        damage_per_second=20.0,    # 压在一根丝上每秒掉多少血。**多根叠加**，
                                   # 所以这个数要按"通常只会压到一两根"来估
        slow_ratio=0.3,            # 压在一根丝上减速多少，0.4 就是速度打六折。
                                   # 多根**不相乘**，取最狠的那一根（见 apply_webs）
    ),
    # 没有撞击伤害这一项：和渔夫、激光、武士一样走基类默认的碰撞效果——
    # 正常弹开、不掉血。输出全在撞墙拉出来的那些丝上，跟撞人没关系
)

# ============================================================
# 死灵法师 —— 撞人按对方**已损**血量咬一口，技能是黑夜降临（换位换血）
# ============================================================
NECROMANCER = Necromancer(
    id="necromancer",
    name="死灵法师",
    radius=22,
    max_hp=500,
    description="残血收割 · 黑夜换命",
    skill=NightfallSkill(
        name="黑夜降临",
        # 冷却**从黑屏结束那一刻**才开始算（它有 duration，见 skills.Skill 的说明），
        # 所以两场黑夜之间实际隔的是 cooldown + duration
        cooldown=20.0,
        duration=1.2,              # 整段黑屏几秒。渐暗/全黑/渐亮三段的比例在
                                   # config/settings.py，换位卡在渐暗走完的那一瞬
    ),
    # 撞一次额外掉的血 = **对方**已损血量 × 这个系数。
    # 注意分子是对方的，不是自己的：对方越残咬得越狠，对方满血时这一下是 0
    missing_hp_damage_ratio=0.35,
)

# ============================================================
# 大锤 —— 碰撞效果无，一把锤子绕身抡，砸中就把人打飞
# ============================================================
HAMMER = Hammer(
    id="hammer",
    name="大锤",
    radius=22,
    max_hp=500,
    description="绕身巨锤 · 一击打飞",
    # 被动技能填在 skill 这一栏（和激光、蜘蛛一样）：它整个角色就这一个能力，
    # 没有主动技能。装锤子走 on_spawn，由 Character.attach_passives 在造球时
    # 两栏一起问（见 base.py）
    skill=HammerSkill(
        name="巨锤",
        # 被动技能用不到这个数（HammerSkill.ready 恒为 False，永远不会被"放"），
        # 但 Skill 基类要求有；填 0 是最不容易误读的
        cooldown=0.0,
        angular_speed=3.2,         # 角速度（弧度/秒）。2π/3.2 ≈ 2 秒抡一圈，
                                   # 也就是每个敌人每 2 秒最多挨一锤
        inner_radius=20.0,         # 柄的内端离球心多远（贴着球面，略小于半径 22）
        outer_radius=72.0,         # 柄的外端，也就是锤头中心离球心多远。
                                   # 这个数同时决定三件事：够不够得着、锤头有
                                   # 多快（ω × 它）、以及被打飞的人飞多远
        head_radius=16.0,          # 锤头多大。**判定用的是它**，不是球半径：
                                   # 贴着球边擦过去的敌人不算挨锤，锤头扫到才算
        damage_per_speed_sq=0.0001,  # 伤害 = 相对速度² × 这个系数。注意是**相对**
                                     # 速度（锤头速度 − 对方速度），对方自己迎面撞
                                     # 上来会更疼
    ),
    # 没有撞击伤害这一项：和渔夫、激光、武士、蜘蛛一样走基类默认的碰撞效果——
    # 正常弹开、不掉血。输出全在锤子上，跟撞人没关系
)

# ============================================================
# 幻影刺客 —— 碰撞无特效，技能闪到敌人身后跟着砍
# ============================================================
PHANTOM = PhantomAssassin(
    id="phantom",
    name="幻影刺客",
    radius=22,
    max_hp=500,
    description="预判闪现 · 贴身追砍",
    # 这是全 roster 里唯一一个**不需要玩家操作**的主动技能：冷却好之后它自己
    # 盯着场上，等预判到自己快挨打了才放（见 skills.BlinkStrikeSkill.ready）。
    # 所以技能条上会出现"冷却满了但没亮"的待发态，那不是 bug
    skill=BlinkStrikeSkill(
        name="幻影突袭",
        cooldown=12.0,             # 从挥砍结束那一刻才开始算（它有 active()，
                                   # 见 Ball.finish_skill）
        react_seconds=0.32,        # 往前预判多久。**别往大里调**：预判是纯外推，
                                   # 看得越远越容易误报（比如对方其实要撞墙了）
        react_samples=8,           # 预判采几个样，见 core/predict.py
        blink_distance=70.0,       # 闪到敌人身后多远。落在对方"运动方向的反面"
        flash_seconds=0.14,        # 闪现那一下的暗角 + 减速持续多久
        slow_factor=0.22,          # 那一下把游戏速度压到几倍（和滑块是相乘的）
        slash_seconds=10,          # 挥砍最多持续多久
        slash_reach=110.0,         # 够得着多远的敌人算砍到
        break_distance=170.0,      # 拉开这么远就收招，冷却开始走
        damage_per_second=55.0,    # 挥砍每秒砍掉多少血
    ),
    # 碰撞效果：无。撞上了就是正常弹开，跟普通小球一样——它的本事全在闪现里
)

# ============================================================
# 毒刺 —— 碰撞效果无，靠撞墙留下的毒刺叠毒，再用毒发兑成伤害和回血
# ============================================================
VENOM = VenomSting(
    id="venom",
    name="毒刺",
    radius=22,
    max_hp=500,
    description="撞墙留刺 · 叠毒爆发",
    # 和武士一样是**两个技能**：被动（刺）填在 passive，主动（毒发）填在 skill
    skill=VirulenceSkill(
        name="毒发",
        cooldown=8.0,              # 一次性结算（duration = 0），放完立刻进冷却
        base_damage=30.0,          # 0 层时也有的一份。所以不是"没毒就白放"
        damage_per_stack=30.0,     # 每层再加这么多
        base_heal=20.0,
        heal_per_stack=15.0,
    ),
    # 常驻被动：不占技能条，也不吃冷却。刺的数值填在这里
    passive=VenomSpikeSkill(
        name="毒刺",
        cooldown=0.0,              # 被动用不到（ready 恒为 False），填 0 最不容易误读
        spike_size=16.0,           # 刺有多"大"：判定上等于给对方的半径加这么多。
                                   # 所以擦着墙从刺旁边过去也算扎到，不用正对着撞
        spike_damage=20.0,         # 扎到那一下的一次性伤害（走普通命中那一套）
        spike_cooldown=1.5,        # 扎完一次要等这么久才能再扎。**这个数决定
                                   # 叠毒速度**：对手在刺旁边待 6 秒，最多叠 4 层
        poison_seconds=20.0,        # 每扎一次叠上去的那层毒活多久
        poison_per_second=3.0,     # **每一层**每秒掉多少血，层层叠加
    ),
    # 没有撞击伤害这一项：和渔夫、激光、武士、蜘蛛、大锤一样走基类默认的
    # 碰撞效果——正常弹开、不掉血。输出全在刺和毒发上，跟撞人没关系
)

# 顺序即右栏角色卡的排列顺序
# ============================================================
# 望 —— 碰撞效果无，靠把战场划成棋盘、不断落子来铺雷区
# ============================================================
LOOKOUT = Lookout(
    id="lookout",
    name="望",
    radius=22,
    max_hp=500,
    description="划分棋盘 · 黑白落子",
    # 棋盘和别的被动一样是**常驻**的，所以填在 skill 这一栏（和激光、蜘蛛、
    # 大锤一样：它整个角色就这一个能力，没有主动技能）。装棋盘走 on_spawn，
    # 由 Character.attach_passives 在造球时两栏一起问（见 base.py）
    skill=GoSkill(
        name="落子",
        # 被动技能用不到这个数（GoSkill.ready 恒为 False，永远不会被"放"），
        # 但 Skill 基类要求有；填 0 是最不容易误读的
        cooldown=0.0,
        # 一手落完之后隔多久起下一手（冷却）。**这是这个角色唯一的节奏旋钮**：
        # 子踩过就消失，棋盘一直半空，所以"同时有几颗子"完全由它决定——间隔
        # 越短，对手每一步越容易踩上。格子是 9×9 共 81 格，一手最多下五颗
        # （一黑四白），铺开的是一片稀疏的雷区
        interval=4.0,
        # 同一手里白子之间隔多久。就是黑子落下之后那四颗白子"唰唰唰唰"铺开
        # 有多快——它只改手感，不改节奏：一轮的长度是 4 × 它 + interval
        white_interval=0.2,
        black_damage=30.0,         # 踩到黑子掉多少血。**黑子是主菜**
        white_damage=15.0,         # 踩到白子掉多少血。白子是配菜——它的用处是
                                   # 把黑子周围那四格也变成雷区，不是它自己疼
        slow_seconds=2.0,          # 踩到之后慢多久。减速**不叠加**（用户定的），
                                   # 连踩两颗是重新计时，不是双倍慢
        slow_ratio=0.45,           # 慢多少，0.45 就是速度打五五折
    ),
    # 没有撞击伤害这一项：和渔夫、激光、武士、蜘蛛、大锤一样走基类默认的
    # 碰撞效果——正常弹开、不掉血。输出全在棋盘上，跟撞人没关系
)

# ============================================================
# 骑士 —— **不是可选角色**，是国王召出来的兵
# ============================================================
# 它单独写在 CHARACTERS 外面，因为它压根不出现在角色卡上：玩家选不了它，它
# 是 KingSkill 生出来的。之所以要按一个完整的角色来写（而不是在国王那里填
# 几个散装数字），是因为它就是**一颗完整的球**——有血量、会飞、撞墙会弹、
# 会被别人的技能打到。血量和半径这些东西只有 Character 上才有地方放。
KNIGHT = Knight(
    id="knight",
    name="骑士",
    radius=12,                     # 半径（像素）。小球，主球 22 的一半多一点。
                                   # 这个数同时是它的碰撞质量口径，也决定它撞墙
                                   # 时能贴多近
    max_hp=30.0,                   # 血量（用户定的 30）。**骑士只有被打死才消失**
                                   # （用户定的），没有存活时长——所以这个数就是
                                   # 它的全部寿命，也是"骑士堆得起来吗"的唯一闸门
    description="国王的骑兵",
    # 骑士没有技能：它的本事全在碰撞里（见下面两个数）。填 IdleSkill 而不是随便
    # 塞一个空 Skill，是为了把"本来就没有"和"忘了写"分开——空 Skill 的 activate
    # 会抛 NotImplementedError，哪天有人把骑士也算进"冷却好了就放"那一趟，
    # 那一帧就会当场炸
    skill=IdleSkill(name="无", cooldown=0.0),
    # 撞上**敌人**：伤对方这么多（高额），自己也掉一点（少量）。
    # 和国王那一条正好反过来——所以骑士是拿去换的，国王本人是躲着走的
    collision_damage=70.0,
    collision_self_damage=8.0,
)

# ============================================================
# 国王 —— 撞人是亏的，输出全靠召出来的骑士
# ============================================================
KING = King(
    id="king",
    name="国王",
    radius=22,
    max_hp=500,
    description="召唤骑士 · 伤敌自损",
    # 一次召三个骑士，360° 均匀散开（用户定的 120° 一个）。之后的推进、碰撞、
    # 死亡全在 Match 那一套里（见 retire_summons），技能本身放完就不管了
    skill=KingSkill(
        name="召唤骑士",
        # **这是唯一一个决定"场上同时有几个骑士"的数**。骑士不会自己消失，
        # 所以调短它等于让骑士一直累加：冷却是 14 秒、一次三个，一分钟之后
        # 场上就是十几个。冷却满了就放，没有别的条件挡着
        cooldown=14.0,
        knight=KNIGHT,
        count=3,                   # 一次召几个。360°/count 一个方向
        launch_speed=320.0,        # 甩出去的初速度（像素/秒）。压在出生速度那
                                   # 一档里（220~380），**不比出生速度快多少**：
                                   # 一出来就冲的话三个骑士是"瞬间糊到脸上"，
                                   # 国王这一手会变成一发爆发，而不是慢慢围上来
    ),
    # 撞上**敌人**：伤对方一点（少量），自己掉更多（较多）。用户定的
    # "伤敌少、自损多"——注意这两笔是各自独立的，和平时"谁撞得狠谁疼"那套
    # 没关系：这两下不看速度、不看血量，撞上就是这个数。
    #
    # 所以国王**不该主动撞人**：它撞一次自己掉的比对方多一倍还多。它的打法是
    # 靠骑士去撞（骑士那条正好是反过来的），本体躲着走
    collision_damage=20.0,
    collision_self_damage=60.0,
)

# 顺序即右栏角色卡的排列顺序
CHARACTERS: tuple = (
    NORMAL, VAMPIRE, FISHER, LASER, SAMURAI, SPIDER, NECROMANCER, HAMMER,
    PHANTOM, VENOM, LOOKOUT, KING,
)
